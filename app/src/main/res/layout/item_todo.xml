<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/transparent">

    <CheckBox
        android:id="@+id/btn_check_todo"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:background="@drawable/check_box_todo"
        android:button="@color/transparent"
        android:buttonTint="@color/transparent"
        android:checked="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_todo_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="9dp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        android:fontFamily="@font/pretendard_bold"
        android:text="title"
        android:textColor="@color/black"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="@+id/btn_check_todo"
        app:layout_constraintStart_toEndOf="@+id/btn_check_todo"
        app:layout_constraintTop_toTopOf="@+id/btn_check_todo" />

    <TextView
        android:id="@+id/tv_todo_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pretendard_regular"
        android:text="@string/todo_time_format"
        android:textColor="@color/mainGray"
        android:textSize="10sp"
        app:layout_constraintStart_toStartOf="@id/tv_todo_title"
        app:layout_constraintTop_toBottomOf="@+id/tv_todo_title" />
</androidx.constraintlayout.widget.ConstraintLayout>